import { styled } from '@mui/material/styles';

const SelectBankCardDiv = styled('div')`
  margin: 16px 0;
`;

const BankCardsDiv = styled('div')`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  margin-top: 12px;
`;

export { SelectBankCardDiv, BankCardsDiv };
